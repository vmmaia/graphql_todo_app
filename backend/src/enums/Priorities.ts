enum Priorities {
    HIGH = 'HIGH',
    MEDIUM = 'MEDIUM',
    LOW = 'LOW',
}

export const priorities: Record<string, Priorities> = {
    HIGH: Priorities.HIGH,
    MEDIUM: Priorities.MEDIUM,
    LOW: Priorities.LOW,
};

export default Priorities;
